apiVersion: v1
kind: Service
metadata:
  name: spring-service-commentaire
  namespace: devops
spec:
  selector:
    app: commentaire-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
      nodePort: 32083
  type: NodePort