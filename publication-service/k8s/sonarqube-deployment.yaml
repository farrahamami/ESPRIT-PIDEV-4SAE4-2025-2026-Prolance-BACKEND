apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarqube-pvc-publication
  namespace: devops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sonarqube-publication
  namespace: devops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sonarqube-publication
  template:
    metadata:
      labels:
        app: sonarqube-publication
    spec:
      automountServiceAccountToken: false   # fix: kubernetes:S6865
      containers:
        - name: sonarqube
          image: sonarqube:10.4.1-community  # fix: kubernetes:S6596 (tag précis au lieu de latest)
          ports:
            - containerPort: 9000
          resources:                         # fix: kubernetes:S6892 / S6873 / S6864
            requests:
              memory: "2Gi"
              cpu: "1000m"
            limits:
              memory: "4Gi"
              cpu: "2"
          readinessProbe:
            httpGet:
              path: /about
              port: 9000
            initialDelaySeconds: 60
            periodSeconds: 15
          volumeMounts:
            - name: sonarqube-data
              mountPath: /opt/sonarqube/data
      volumes:
        - name: sonarqube-data
          persistentVolumeClaim:
            claimName: sonarqube-pvc-publication
---
apiVersion: v1
kind: Service
metadata:
  name: sonarqube-service-publication
  namespace: devops
spec:
  selector:
    app: sonarqube-publication
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
      nodePort: 32001
  type: NodePort
